---
sidebar_position: 1
sidebar_label: Array 数组
description: JavaScript 数组用于在单一变量中存储多个值
title: Arrary 数组
---

## Array.prototype.at()

`at()` 方法接收一个整数值并返回该索引对应的元素，允许正数和负数

负整数从数组中的最后一个元素开始倒数

```jsx live noInline
const array1 = [5, 12, 8, 130, 44]

let index = 2

render(`Using an index of ${index} the item returned is ${array1.at(index)}`)
// Expected output: "Using an index of 2 the item returned is 8"
```

<br />

## Array.prototype.concat()

`concat()` 方法用于合并两个或多个数组

此方法不会更改现有数组，而是返回一个新数组

```jsx live noInline
const array1 = ["a", "b", "c"]
const array2 = ["d", "e", "f"]
const array3 = array1.concat(array2)

// Expected output: Array ["a", "b", "c", "d", "e", "f"]
render(
  <ul>
    {array3.map((item, index) => (
      <li key={index}>{item}</li>
    ))}
  </ul>
)
```
